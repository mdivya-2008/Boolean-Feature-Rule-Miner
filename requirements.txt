# Boolean Feature Rule Miner — pinned dependencies (Python 3.10+)
# Install:  pip install -r requirements.txt
numpy==2.3.5
pandas==2.2.3
matplotlib==3.10.9
streamlit==1.64.0

# --- optional: only needed to (re)generate the demo notebook outputs ---
# nbformat==5.10.4
# nbclient==0.11.0
# ipykernel==6.31.0
